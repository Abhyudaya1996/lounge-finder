import { rest } from 'msw';

// TODO: Populate with mock API handlers for cards, lounges, etc.
export const handlers = [
  // Example:
  // rest.get('/api/cards', (req, res, ctx) => {
  //   return res(ctx.status(200), ctx.json([]));
  // }),
] as const; 